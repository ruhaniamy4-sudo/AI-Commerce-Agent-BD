'use client';
import Link from 'next/link'; import { usePathname, useRouter } from 'next/navigation'; import { Activity, Building2, ChartNoAxesCombined, HeartPulse, LayoutDashboard, LogOut, ShieldCheck, Users } from 'lucide-react'; import { cn } from '@/lib/utils';
const items=[['Overview','/platform-admin',LayoutDashboard],['Businesses','/platform-admin/businesses',Building2],['Users','/platform-admin/users',Users],['AI Usage','/platform-admin/usage',ChartNoAxesCombined],['Integrations','/platform-admin/integrations',Activity],['System Health','/platform-admin/health',HeartPulse],['Errors / Logs','/platform-admin/errors',ShieldCheck]] as const;
export function PlatformShell({children}:{children:React.ReactNode}){const pathname=usePathname();const router=useRouter();async function logout(){await fetch('/api/platform-auth/logout',{method:'POST'});router.push('/admin/login');router.refresh();}
return <div className="min-h-screen bg-slate-950 text-slate-100"><header className="border-b border-slate-800 bg-slate-900/80 p-3 lg:hidden"><div className="mb-3 flex items-center justify-between"><b>SellPilot Platform</b><button onClick={logout}><LogOut className="h-4 w-4"/></button></div><nav className="flex gap-2 overflow-x-auto pb-1">{items.map(([name,href,Icon])=><Link key={href} href={href} className={cn('flex shrink-0 items-center gap-2 rounded-lg px-3 py-2 text-xs text-slate-400',pathname===href&&'bg-violet-600 text-white')}><Icon className="h-3.5 w-3.5"/>{name}</Link>)}</nav></header><div className="mx-auto flex min-h-screen max-w-[1800px]"><aside className="hidden w-64 shrink-0 border-r border-slate-800 bg-slate-900 p-5 lg:flex lg:flex-col"><div className="mb-8 flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-violet-600"><ShieldCheck className="h-5 w-5"/></div><div><b>SellPilot</b><p className="text-xs text-slate-400">Platform Operations</p></div></div><nav className="flex-1 space-y-1">{items.map(([name,href,Icon])=><Link key={href} href={href} className={cn('flex items-center gap-3 rounded-lg px-3 py-2 text-sm text-slate-400 hover:bg-slate-800 hover:text-white',pathname===href&&'bg-violet-600 text-white')}><Icon className="h-4 w-4"/>{name}</Link>)}</nav><button onClick={logout} className="flex items-center gap-3 rounded-lg px-3 py-2 text-sm text-slate-400 hover:bg-slate-800 hover:text-white"><LogOut className="h-4 w-4"/>Sign out</button></aside><main className="min-w-0 flex-1 p-4 md:p-8">{children}</main></div></div>}
