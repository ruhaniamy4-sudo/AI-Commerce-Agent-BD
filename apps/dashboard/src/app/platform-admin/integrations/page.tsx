'use client';
import { useQuery } from '@tanstack/react-query'; import { platformApi } from '@/lib/platform-api'; import { Card,CardContent,CardHeader,CardTitle } from '@/components/ui/card';
export default function Integrations(){const {data,isLoading}=useQuery({queryKey:['platform-integrations'],queryFn:platformApi.integrations});return <div className="space-y-6"><div><h1 className="text-3xl font-bold">Channels / Integrations</h1><p className="text-slate-400">Connection counts only; provider and merchant secrets are never returned.</p></div>{isLoading?<p>Loading…</p>:<div className="grid gap-4 md:grid-cols-2"><Group title="Business channels" rows={data?.channels||[]}/><Group title="Courier integrations" rows={data?.couriers||[]}/></div>}</div>}
function Group({title,rows}:{title:string;rows:Array<{_id:Record<string,string>;count:number}>}){return <Card className="border-slate-800 bg-slate-900 text-white"><CardHeader><CardTitle>{title}</CardTitle></CardHeader><CardContent className="space-y-3">{rows.map((row,i)=><div className="flex justify-between rounded-lg border border-slate-800 p-3" key={i}><span>{Object.values(row._id).join(' · ')}</span><b>{row.count}</b></div>)}{!rows.length&&<p className="text-slate-400">No configured integrations.</p>}</CardContent></Card>}
