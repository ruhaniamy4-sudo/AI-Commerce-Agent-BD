'use client';import {useState} from 'react';import {useMutation,useQuery,useQueryClient} from '@tanstack/react-query';import {platformApi} from '@/lib/platform-api';import {Input} from '@/components/ui/input';import {Badge} from '@/components/ui/badge';import {Button} from '@/components/ui/button';
export default function Users(){const [search,setSearch]=useState('');const qc=useQueryClient();const {data,isLoading}=useQuery({queryKey:['platform-users',search],queryFn:()=>platformApi.users(search)});const change=useMutation({mutationFn:({id,name,status}:{id:string;name:string;status:'active'|'disabled'})=>{if(!confirm(`${status==='disabled'?'Suspend':'Reactivate'} user ${name}?`))throw new Error('Cancelled');const reason=prompt('Reason (required)')?.trim();if(!reason)throw new Error('Reason required');return platformApi.setUserStatus(id,status,reason)},onSuccess:()=>qc.invalidateQueries({queryKey:['platform-users']})});return <div className="space-y-6"><div><h1 className="text-3xl font-bold">Users</h1><p className="text-slate-400">Merchant accounts, business roles, account status, and recorded activity. Credentials are never exposed.</p></div><Input className="max-w-md border-slate-700 bg-slate-900" placeholder="Search name or email" value={search} onChange={e=>setSearch(e.target.value)}/>{isLoading?<p>Loading…</p>:<div className="overflow-x-auto rounded-xl border border-slate-800"><table className="w-full min-w-[950px] text-sm"><thead className="bg-slate-900 text-left text-slate-400"><tr>{['User','Business / role','Account status','Last active','Created','Action'].map(x=><th className="p-4" key={x}>{x}</th>)}</tr></thead><tbody>{data?.data.map(u=><tr key={u._id} className="border-t border-slate-800"><td className="p-4"><b>{u.name}</b><p className="text-slate-500">{u.email}</p></td><td className="p-4">{u.memberships.map(m=><p key={`${m.business?._id}-${m.role}`}>{m.business?.name||'Unknown'} · {m.role}</p>)}</td><td className="p-4"><Badge variant={u.status==='active'?'default':'destructive'}>{u.status}</Badge></td><td className="p-4">{u.lastSeenAt?new Date(u.lastSeenAt).toLocaleString():'Never recorded'}</td><td className="p-4">{new Date(u.createdAt).toLocaleDateString()}</td><td className="p-4"><Button variant="outline" size="sm" onClick={()=>change.mutate({id:u._id,name:u.name,status:u.status==='active'?'disabled':'active'})}>{u.status==='active'?'Suspend':'Reactivate'}</Button></td></tr>)}</tbody></table></div>}</div>}
