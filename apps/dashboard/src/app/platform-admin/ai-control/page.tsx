'use client';import {useState} from 'react';import {useMutation,useQuery,useQueryClient} from '@tanstack/react-query';import {platformApi} from '@/lib/platform-api';import {Input} from '@/components/ui/input';import {Button} from '@/components/ui/button';import {Badge} from '@/components/ui/badge';
export default function AIControl(){const [search,setSearch]=useState('');const qc=useQueryClient();const {data}=useQuery({queryKey:['ai-control',search],queryFn:()=>platformApi.businesses(search,'',1)});const change=useMutation({mutationFn:({id,name,suspended}:{id:string;name:string;suspended:boolean})=>{if(!window.confirm(`${suspended?'Suspend':'Resume'} AI for ${name}? This affects future automated responses only.`))throw new Error('Cancelled');const reason=window.prompt('Reason (required)')?.trim();if(!reason)throw new Error('Reason required');return platformApi.setAIStatus(id,suspended?'SUSPENDED_BY_PLATFORM':'ENABLED',reason)},onSuccess:()=>qc.invalidateQueries({queryKey:['ai-control']})});return <div className="space-y-6"><div><h1 className="text-3xl font-bold">AI Control</h1><p className="text-slate-400">Business-level AI access. This is separate from human takeover and business status.</p></div><Input className="max-w-md border-slate-700 bg-slate-900" placeholder="Search businesses" value={search} onChange={e=>setSearch(e.target.value)}/><div className="overflow-x-auto rounded-xl border border-slate-800"><table className="w-full min-w-[850px] text-sm"><thead className="bg-slate-900 text-left text-slate-400"><tr>{['Business','Business status','Subscription','AI status','Requests this month','Cost','Action'].map(x=><th className="p-4" key={x}>{x}</th>)}</tr></thead><tbody>{data?.data.map(b=>{const suspended=b.aiAccess?.status==='SUSPENDED_BY_PLATFORM';return <tr className="border-t border-slate-800" key={b._id}><td className="p-4 font-semibold">{b.name}</td><td className="p-4"><Badge>{b.status}</Badge></td><td className="p-4">{b.subscription?.status||'Not configured'}</td><td className="p-4"><Badge variant={suspended?'destructive':'secondary'}>{b.aiAccess?.status||'ENABLED'}</Badge></td><td className="p-4">{b.usage.requests}</td><td className="p-4">{b.usage.unknown?'Unavailable':`$${b.usage.cost.toFixed(4)}`}</td><td className="p-4"><Button variant="outline" onClick={()=>change.mutate({id:b._id,name:b.name,suspended:!suspended})}>{suspended?'Resume AI':'Suspend AI'}</Button></td></tr>})}</tbody></table></div></div>}
