'use client';import {useState} from 'react';import {useQuery} from '@tanstack/react-query';import {platformApi} from '@/lib/platform-api';import {Input} from '@/components/ui/input';
export default function Audit(){const [search,setSearch]=useState('');const [action,setAction]=useState('');const {data}=useQuery({queryKey:['audit',search,action],queryFn:()=>platformApi.audit(search,action)});return <div className="space-y-6"><div><h1 className="text-3xl font-bold">Audit Log</h1><p className="text-slate-400">Security-sensitive platform actions with actor, target, reason, and timestamp.</p></div><div className="flex gap-3"><Input className="max-w-md border-slate-700 bg-slate-900" placeholder="Search action, target, or reason" value={search} onChange={e=>setSearch(e.target.value)}/><select className="rounded-md border border-slate-700 bg-slate-900 px-3" value={action} onChange={e=>setAction(e.target.value)}><option value="">All actions</option>{['BUSINESS_SUSPENDED','BUSINESS_REACTIVATED','AI_SUSPENDED','AI_RESUMED','USER_SUSPENDED','USER_REACTIVATED','NEW_SUBSCRIPTION','RENEWAL','MANUAL_BILLING_ADJUSTMENT','ADMIN_LOGIN'].map(x=><option key={x}>{x}</option>)}</select></div><div className="overflow-x-auto rounded-xl border border-slate-800"><table className="w-full min-w-[1000px] text-sm"><thead className="bg-slate-900 text-left text-slate-400"><tr>{['Time','Action','Business','Target','Administrator','Reason'].map(x=><th className="p-4" key={x}>{x}</th>)}</tr></thead><tbody>{data?.data.map(row=><tr key={row._id} className="border-t border-slate-800"><td className="p-4">{new Date(row.createdAt).toLocaleString()}</td><td className="p-4 font-semibold">{row.action}</td><td className="p-4">{row.business?.name||'—'}</td><td className="p-4">{row.targetType} · {row.targetId}</td><td className="p-4">{row.admin?.name||'Unknown'}<p className="text-xs text-slate-500">{row.admin?.email}</p></td><td className="p-4">{row.reason}</td></tr>)}</tbody></table></div></div>}
