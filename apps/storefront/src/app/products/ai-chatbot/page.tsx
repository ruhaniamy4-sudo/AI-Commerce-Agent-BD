import { ProductExperience } from "@/components/commerce-system";
export default function Page() { return <ProductExperience kind="chat"/>; }
import type { Metadata } from "next";
export const metadata: Metadata = { title: "AI Chatbot", description: "A business-aware AI assistant for customer conversations, product discovery, and sales." };
