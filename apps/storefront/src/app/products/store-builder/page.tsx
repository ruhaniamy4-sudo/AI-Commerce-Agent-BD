import { ProductExperience } from "@/components/commerce-system";
export default function Page() { return <ProductExperience kind="store"/>; }
import type { Metadata } from "next";
export const metadata: Metadata = { title: "Store Builder", description: "Create a connected storefront with your SellPilot products and checkout." };
