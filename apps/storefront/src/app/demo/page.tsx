import type { Metadata } from "next";
import { CalendarCheck, CheckCircle2, MessageCircle, ShieldCheck } from "lucide-react";
import type { LucideIcon } from "lucide-react";
import { DemoForm } from "@/components/demo-form";
import { PageHero } from "@/components/marketing";
export const metadata: Metadata = { title: "Request a Demo", description: "Request an early-access Digitross AI commerce walkthrough for your Bangladesh business." };
const demoPoints: Array<[LucideIcon, string, string]> = [[MessageCircle, "Your conversation channels", "Where customers ask, compare, and decide."], [CalendarCheck, "A realistic rollout", "Which workflow to test first and how to measure it."], [ShieldCheck, "Control and safety", "Human takeover, tenant boundaries, and AI cost guardrails."]];
export default function DemoPage() { return <main><PageHero eyebrow="See Digitross in context" title="Request an early-access walkthrough." copy="Tell us how customers reach your business. We’ll focus the conversation on your current sales and support operation." /><section className="site-container grid gap-10 py-20 lg:grid-cols-[.42fr_.58fr]"><div><p className="section-kicker">A focused conversation</p><h2 className="mt-4 text-2xl font-semibold tracking-tight text-[var(--ink)]">What we’ll cover</h2><div className="mt-7 space-y-5">{demoPoints.map(([Icon, title, copy]) => <div key={title} className="flex gap-4"><div className="icon-tile shrink-0"><Icon className="h-5 w-5" /></div><div><h3 className="font-semibold text-[var(--ink)]">{title}</h3><p className="mt-1 text-sm leading-6 text-[var(--muted)]">{copy}</p></div></div>)}</div><div className="mt-8 rounded-2xl border border-emerald-500/20 bg-emerald-500/[.06] p-5 text-sm leading-6 text-[var(--muted)]"><CheckCircle2 className="mb-3 h-5 w-5 text-emerald-500" />This public form prepares a demo brief locally. It does not transmit your information.</div></div><DemoForm /></section></main>; }
